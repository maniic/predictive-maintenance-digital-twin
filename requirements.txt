# Runtime dependencies, mirroring [project.dependencies] in pyproject.toml.
#
# Prefer `pip install -e .` — this file exists for environments that cannot
# install the project as a package. Optional extras:
#   pip install -e ".[tracking]"   MLflow experiment tracking
#   pip install -e ".[explain]"    SHAP / matplotlib explainability
#   pip install -e ".[dev]"        pytest, ruff
#
# For a byte-for-byte reproducible environment, use requirements.lock.txt.

pandas>=2.0.0,<4
numpy>=1.24.0,<3
scikit-learn>=1.3.0,<2
torch>=2.0.0,<3
pytorch-lightning>=2.0.0,<3
pyyaml>=6.0.0,<7
